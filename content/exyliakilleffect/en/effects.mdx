---
title: "Effects and categories"
description: "How effects.yml is written, and the 120 effects that ship in it."
---

## The file

`effects.yml` has two sections: the categories, and the effects that belong to them.

```yaml title="effects.yml"
categories:
  siege:
    material: NETHERITE_SWORD
    name: 'Siege'
    priority: 1

kill_effects:
  BLADE_STORM:
    category: siege
    name: "<gradient:#DCE6F2:#8FA6C4><bold>BLADE STORM</bold></gradient>"
    material: NETHERITE_SWORD
    description:
      - 'Twelve blades come down point first, stand'
      - 'for a beat, then {highlight}close all at once{neutral}.'
    priority: 1
    effects:
      - '[SOUND] ENTITY_BREEZE_INHALE;1.5;1.7'
      - '[CIRCLE] NETHERITE_SWORD;as:item;radius:3.4;points:12;from:0,10,0;to:0,1.0,0;ease:in;life:0.55;roll:135;face_out:true;size:1.4;light:15'
      - '[DELAY] 0.58'
      - '[NPC] {victim};pose:lying;life:2.2;equip:true;face:false'
```

| Key | What it does |
|---|---|
| `category` | Which tab it appears under. Must be a key declared under `categories`. |
| `name` | What menus and placeholders call it. Palette tokens, MiniMessage and gradients all work. |
| `material` | The item the row is drawn with. Defaults to `BARRIER`. |
| `description` | A list is one line per entry; a single string breaks on `<nl>`. |
| `priority` | Where it sits in its tab; lower comes first. Defaults to `999`. |
| `effects` | The steps that are drawn where the victim fell. |
| `weapons` | Optional. What the effect may be bound to. Omit it and it fits anything. |

A category takes `material`, `name` and `priority`.

<Callout type="note" title="Broken entries are reported, not guessed">
A missing `categories` section, an effect naming a category that is not declared, an entry with no
`effects`, and a `weapons` token that is neither an item nor a family are each written to the console
with the key that caused them. The rest of the file still loads.
</Callout>

## `weapons`

Only read while `behaviour.mode` reads weapons. It accepts item names and families:

```yaml
weapons: [ NETHERITE_SWORD, AXE ]
```

| Family | Matches |
|---|---|
| `ANY` | Anything at all. |
| `SWORD` | Every `_SWORD`. |
| `AXE` | Every `_AXE` — a pickaxe is not one. |
| `TRIDENT`, `MACE`, `BOW`, `CROSSBOW` | Just that item. |
| `MELEE` | Swords, axes, tridents and maces. |
| `RANGED` | Bows, crossbows and tridents. |

Families are matched on the item's name, so a sword added in a future Minecraft version is a sword the
day it exists. `ANY_SWORD` reads the same as `SWORD`. None of the shipped effects declares `weapons`,
so out of the box every one of them fits every weapon.

<Callout type="note" title="The effect id is the permission">
`BLADE_STORM` is `exyliakilleffect.effect.blade_storm`. Renaming the key renames the node, and
everybody who had the old one loses the effect. Case is not part of it: ids are lowercased on the way
in.
</Callout>

## The catalogue

Twenty effects per category, 120 in total.

### Siege — `NETHERITE_SWORD`

Weapons, engines and the weight of things arriving.

`BLADE_STORM` `JUDGEMENT` `GUILLOTINE` `SIEGE_VOLLEY` `COLOSSUS_FIST` `PHALANX` `TREBUCHET`
`CROSSFIRE` `WAR_DRUM` `CHAIN_SNARE` `EXECUTIONER` `BALLISTA` `SPEAR_WALL` `IRON_MAIDEN` `CANNONADE`
`RAMPART` `SHIELD_BREAK` `BLADE_DANCE` `ARTILLERY` `LAST_STAND`

### Cataclysm — `MAGMA_BLOCK`

Fire, rock and weather.

`METEOR_STRIKE` `CINDER_COLLAPSE` `VOLCANIC_VENT` `PYROCLAST` `MAGMA_TIDE` `EMBER_SWARM` `FISSURE`
`INFERNO_PILLAR` `ASHFALL` `QUAKE` `ROCKSLIDE` `SOLAR_FLARE` `CRATER` `FIREWHIRL` `OBSIDIAN_SPIKE`
`SCORCH` `THUNDER_ANVIL` `STORMFRONT` `HAILSTORM` `TECTONIC`

### Abyss — `CRYING_OBSIDIAN`

Void and gravity.

`VOID_IMPLOSION` `BLACK_HOLE` `OBSIDIAN_MAUSOLEUM` `SOUL_HARVEST` `EVENT_HORIZON` `NULL_FIELD`
`WRAITH` `GRAVEWELL` `SHADOW_HANDS` `ENTROPY` `ECHO_CHAMBER` `UNMAKING` `THE_QUIET` `TETHER`
`ABYSSAL_MAW` `COLD_STAR` `REVENANT` `SINGULARITY_LANCE` `FADE` `OBLIVION_GATE`

### Aurora — `AMETHYST_CLUSTER`

Crystal, starlight and cold.

`CRYSTAL_SHATTER` `FROZEN_TOMB` `STARFALL` `SUPERNOVA` `PRISM` `AURORA_VEIL` `GLACIER` `COMET`
`CONSTELLATION` `LUMEN` `SHARD_RAIN` `ORBITAL` `FROSTBLOOM` `NORTHERN_LIGHT` `SOLSTICE` `SNOWGLOBE`
`ZENITH` `REFRACTION` `MOONFALL` `STARLIGHT_CHOIR`

### Verdant — `SPORE_BLOSSOM`

Growth, petals and rot.

`VENOM_BLOOM` `SAKURA_FALL` `OVERGROWTH` `THORNCAGE` `SPORE_BURST` `ROOTGRASP` `MYCELIUM`
`PETAL_STORM` `WITHER_ROT` `HONEY_TRAP` `BAMBOO_STRIKE` `LOTUS` `SWARM` `TERRARIUM` `AUTUMN`
`VINE_SNARE` `GARDEN_OF_BONES` `SEEDFALL` `NIGHT_ORCHID` `MOSS_CLAIM`

### Bonk — `ANVIL`

Jokes.

`THE_CLASSIC` `BONK` `ANVIL_CHORUS` `PIANO` `SKILL_ISSUE` `GG_EZ` `YEET` `RUBBER_DUCK` `CAKE`
`TNT_DELIVERY` `FISH_SLAP` `BIG_BOOT` `FRYING_PAN` `BANANA_PEEL` `CHICKEN_JOCKEY` `SAFE_DELIVERY`
`TOO_SLOW` `RATIO` `BALLOON_POP` `VENDING_MACHINE`

## The body on the ground

107 of the 120 effects use the `[NPC]` step: a copy of the victim, wearing what they wore, left
lying — or thrown, or slumping — where they fell for a couple of seconds. It is a display, not an entity anyone can hit — nothing can
be looted from it and nothing collides with it.

## Writing your own

<Steps>
  <Step title="Add the entry">
    A new key under `kill_effects`, with a `category` that exists.
  </Step>
  <Step title="Write the steps">
    A sound, a shape, a delay. The full syntax is in [Sequences](/docs/exyliakilleffect/sequences).
  </Step>
  <Step title="Reload">
    `/killeffectsadmin reload`.
  </Step>
  <Step title="Look at it">
    `/killeffect preview <id>` plays it against an empty sky, then puts you back.
  </Step>
  <Step title="Grant it">
    `exyliakilleffect.effect.<id in lowercase>`, or the whole category.
  </Step>
</Steps>
